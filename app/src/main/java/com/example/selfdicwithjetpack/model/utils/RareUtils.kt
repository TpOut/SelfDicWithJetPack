package com.example.selfdicwithjetpack.model.utils

import android.app.Activity
import android.app.ActivityManager
import android.graphics.BitmapFactory
import android.view.KeyboardShortcutGroup
import android.view.Menu
import android.view.View

/**
 * Created by TpOut on 2020/10/27.<br>
 * Email address: 416756910@qq.com<br>
 */
class RareUtils {

//    // 必须在onCreate 里创建，是否能够自定义取决于系统
//    // 单独作为一个栈
//    // 模拟器没有名字，而小米和华为的图标不会更新成这里设置的
//    fun setIconInRecentTask(drawableId: Int, colorId: Int, activity: Activity) {
//        var taskDescription = ActivityManager.TaskDescription(
//            "哈哈哈",
//            BitmapFactory.decodeResource(resources, drawableId),
//            resources.getColor(colorId)
//        )
//        activity.setTaskDescription(taskDescription) //设置颜色，标签，图标等
//    }
//
//    fun onProvideKeyboardShortcuts(data: MutableList<KeyboardShortcutGroup>?, menu: Menu?, deviceId: Int) {
//        super.onProvideKeyboardShortcuts(data, menu, deviceId)
//    }
//
//    // onProvideKeyboardShortcuts() 方法可以提供
//    fun shortcut(v: View) {
//        requestShowKeyboardShortcuts()
//    }
//
//    fun pin(v: View) {
//        // 屏幕锁定，
//        // 效果是最近列表里不展示堆栈(因为堆栈也被固定），且无法返回退出应用，home 后无法打开其他应用等
//        if (!lock) {
//            startLockTask()
//        } else {
//            stopLockTask()
//        }
//        lock = !lock
//        // 管理可以使用
////        setLockTaskPackages()
//    }


}